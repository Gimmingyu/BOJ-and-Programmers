def solution(s):
    answer = ''
    
    num_list = []
    str_list = s.split()
    
    for i in str_list:
        num = int(i)
        num_list.append(num)
    
    return str(min(num_list))+" "+str(max(num_list))